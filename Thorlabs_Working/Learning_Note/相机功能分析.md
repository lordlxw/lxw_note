> ThorCam 和 ThorImageCam

# ThorCam

> 



# CM501

> 用于椭圆偏振测量（Ellipsometry）应用。CM501是一个专业的偏振测量设备，能够测量样品的偏振特性。
>
> 1、设备连接
>
> 电脑 → 串口连接 → CM501硬件设备
> 电脑 → USB连接 → TSI相机
>
> 2、相机作用和目的
>
> 相机的作用：
>
> - 接收从样品反射回来的光
>
> - 将光信号转换为数字图像
>
> - 记录光的强度和分布
>
>
> MainContent其实是四幅画面
>
> ```xaml
> <telerik:RadNavigationView.Content>
>     <Grid>
>         <ContentControl x:Name="cameraview" />
>         <ContentControl x:Name="d1view" />
>         <ContentControl x:Name="d2view" />
>         <ContentControl x:Name="d3view" />
>     </Grid>
> </telerik:RadNavigationView.Content>
> ```
>
> ### 多边形/圆形 ROI 怎么实现？
>
> 通常是在 **后台代码（软件层）** 里做的，比如：
>
> 1. **先采集矩形 ROI**（保证数据尽量少）
>    - 如果你要圆形，通常还是取一个能包含圆的矩形。
> 2. **再做掩膜（mask）处理**
>    - 在代码里生成一个圆形/多边形的 mask（掩膜图，像素值=0或1）。
>    - 用掩膜去过滤图像，非 ROI 区域设为黑色或直接忽略。
>
>
> 需要掩膜的：
>
> 圆形 以及 多边形
>
> 原因：
>
> - 相机只能设置矩形ROI
>
> - 需要在矩形区域内进一步限制为圆形/多边形
>
> - 需要软件掩膜过滤圆外的像素/多边形外的像素

## Coordinate 

> ## Coordinate 的作用
>
> ### 1. Coordinate 的本质
>
> Coordinate 是一个坐标转换管理器，负责处理不同坐标系统之间的转换：
>
> - 屏幕坐标：鼠标在屏幕上的像素位置
>
> - 物理坐标：图像在物理空间中的位置
>
> - 像素坐标：图像像素的索引位置
>
> - 世界坐标：应用程序逻辑中的坐标



## ROI变换

> ImageDataToWriteableBitmap
>
> 核心方法转图片
>
> 第一步：ArrayCopy原图片，得到imageData
>
> 第二步：检查是否旋转，进行像素变换，传入imageData得到transformedImageData
>
> 第三步：检查是否翻转，进行像素变化，传入transformedImageData得到新的transformedImageData
>
> 第四步：把数据赋值给ArrayCopy(transformedImageData, CameraData.Instance.TransformedLastImage, width, height);
>
> 第五步：检查模式（多边形/矩形还是圆形），赋值data，然后将多余部分像素设为0（黑）
>
> * 先是矩形裁剪（硬件）
>
> * 再是圆形掩膜（软件）
>
> * *// 第一步：设置相机硬件ROI*
>
>   SetCameraROI(ref regionStartPointX, ref regionStartPointY, ref regionWidth, ref regionHeight);
>
>   *// ↑ 这里设置相机的矩形ROI区域，硬件层面先裁剪*
>
>   *// 第二步：绘制图像*
>
>   DrawImage(true);
>
>   *// ↑ 这里调用图像处理，包括软件层面的圆形/多边形掩膜*



## DrawingCanvas 和 CurrentTool密切相关

> ```xaml
>  <drawingtool:DrawingCanvas x:Name="drawingCanvas"
>                             Background="Transparent"
>                             Coordinate="{Binding Coordinate}"
>                             CurrentTool="{Binding CurrentTool}"
>                             GraphicFactory="{StaticResource Factory}"
>                             Items="{Binding ROIs}"
>                             MouseDown="DrawingCanvas_OnMouseDown"
>                             MouseLeave="DrawingCanvas_MouseLeave"
>                             MouseMove="DrawingCanvas_MouseMove"
>                             PreviewMouseDown="DrawingCanvas_PreviewMouseDown"
>                             SizeChanged="DrawingCanvas_SizeChanged">
> ```
>
> 
>
> Canvas的事件---》传给CurrentTool，比如MouseDown，MouseMove等



> 图形是如何展示到画布上的？
>
> - 调用链你贴的那段 Rendering 
>
> - Rendering 中从队列取到 RectangleViewModel → GraphicFactory.GetGraphic(...) 得到 GraphicRectangleEx
>
> - 设置 Coordinate/Clip 后调用 graphic.Draw() → 进 GraphicRectangleEx.DrawROI(...) → 用 DrawingContext.DrawRectangle(...) 画出虚线框
>
> 结论：矩形的 UI 是在 GraphicRectangleEx.DrawROI 里画的；Factory.GetGraphic 负责把 RectangleViewModel 映射为对应的绘制类。
>
> ---
>
> CurrentTool
>
> - 决定“谁来处理鼠标事件”。工具在 MouseDown/Move/Up 里创建/更新 ROI 的 ViewModel（如 RectangleViewModel），并通过事件让 VM 把它加进 ROIs。
>
> Items="{Binding ROIs}"
>
> - 把 VM 的 ROIs 集合绑定到画布。集合变更（Add/Remove）会被 CanvasBase.ItemsPropertyChangedCallback/Items_CollectionChanged 监听，并把增删项分别放入 newGraphics/oldGraphics 队列。
>
> GraphicFactory="{StaticResource Factory}"
>
> - 真正“触发”的位置在 CompositionTarget_Rendering（每帧渲染回调）：
>
> - 从队列取出待新增的 VM → 调用 GraphicFactory.GetGraphic(vm) → 映射成具体绘制类（如 GraphicRectangleEx）
>
> - 设置 Coordinate/Clip，然后 graphic.Draw() → 在画布上画出来并加入 VisualCollection
>
> - 待删除的 VM 从 itemsContainer/VisualCollection 移除



> 枚举值：
>
> ### 1. Flip（翻转状态）
>
> - None = 0：不翻转
>
> - Horizon = 1：水平翻转（左右镜像）
>
> - Vertical = 2：垂直翻转（上下镜像）
>
> - Both = 3：水平+垂直翻转
>
> ### 2. ImageRotation（旋转角度）
>
> - deg0 = 0：0度（原始方向）
>
> - deg90 = 1：顺时针90度
>
> - deg180 = 2：180度
>
> - deg270 = 3：顺时针270度（逆时针90度）
>
> ### 3. GestureType（坐标系统手势类型）
>
> - LT = 0：原点在左上角，顺时针
>
> - AntLT = 1：原点在左上角，逆时针
>
> - RT = 2：原点在右上角，逆时针
>
> - AntRT = 3：原点在右上角，顺时针
>
> - LB = 4：原点在左下角，逆时针
>
> - AntLB = 5：原点在左下角，顺时针
>
> - RB = 6：原点在右下角，逆时针
>
> - AntRB = 7：原点在右下角，顺时针
>
> 3. ROIMode（过滤的模式）
>
> - Mode="0"：矩形ROI模式
>
> - Mode="1"：圆形ROI模式 ← 你问的这个
>
> - Mode="2"：多边形ROI模式
>
> 
> 



## 问题

> 1、相机过曝 什么时候会显示？





## 算法

> 1、测量偏振的 5帧测量法？？
>
> 2、掩膜核心算法
>
> SetCameraROI方法
>
> 核心算法：GenerateMaskData



# 不懂点

> 1、DCC、DCU 和 CCD  啥意思
>
> ## CCD (Charge-Coupled Device)
>
> 电荷耦合器件，这是最广为人知的：
>
> - 一种图像传感器技术，用于将光信号转换为电信号
>
> - 通过光电效应将光子转换为电子，然后逐行传输电荷
>
> - 曾是数码相机的主流传感器技术，现在主要被 CMOS 替代
>
> - 具有高图像质量、低噪声等优点，但功耗较高
>
> ## DCU (Digital Control Unit)
>
> 数字控制单元：
>
> - 负责控制相机各个功能模块的微处理器
>
> - 管理自动对焦、曝光控制、白平衡等参数
>
> - 处理用户界面操作和菜单控制
>
> - 协调相机各个组件之间的通信
>
> ## DCC (Dynamic Contrast Control)
>
> 动态对比度控制：
>
> - 一种图像处理技术，用于优化图像的对比度
>
> - 根据场景内容自动调整亮部和暗部的细节表现
>
> - 提高图像的整体视觉效果和细节层次
>
> - 在拍摄高对比度场景时特别有用
>
> 这三个技术共同构成了现代数码相机的核心技术架构，CCD 负责图像采集，DCU 负责系统控制，DCC 负责图像优化处理。





> 2、ROI什么意思
>
> 在图像处理/相机领域：
>
> - **ROI（Region of Interest）**：图像中你特别关注或要处理的部分区域。
> - **作用**：只对 ROI 区域进行处理或分析，可以提高效率，减少计算量。
>
> 举例：
>
> - 你用相机拍摄一张 1920×1080 的图片，但只想分析图片中间的一个 500×500 像素的区域，这个区域就是 ROI。







> 3、直方图（Histogram）
>
> ![image-20250825110200833](C:\Users\root\AppData\Roaming\Typora\typora-user-images\image-20250825110200833.png)
>
> - **Histogram（直方图）** 是一种图形表示方法，用来显示 **图像中各像素亮度或颜色分布的频率**。
> - 横轴表示 **像素值**（亮度或颜色等级），纵轴表示 **该像素值出现的次数**。
>
> ------
>
> ### 用途
>
> 1. **观察图像曝光**：
>    - 左边集中 → 图像偏暗（欠曝）
>    - 右边集中 → 图像偏亮（过曝）
>    - 中间分布 → 曝光正常
> 2. **调整图像**：根据直方图调整曝光、对比度或亮度。
>
> 直方图用来显示图像中所有像素的亮度分布情况：
>
> - X轴：像素亮度值（0-1023，10位深度）
>
> - Y轴：每个亮度级别对应的像素数量
>
> ------
>
> ### 举例
>
> - 一张灰度图像，像素值从 0（黑）到 255（白）：
>   - 直方图高峰在 200 → 图片整体偏亮
>   - 高峰在 50 → 图片整体偏暗





> 4、Intensity —— 强度
>
> ### Intensity（强度）
>
> - 定义：像素的亮度值，范围从0到1023
>
> - 具体含义：
>
> - 0 = 纯黑色（最暗）
>
> - 1023 = 纯白色（最亮）
>
> - 95 = 中等偏暗的灰度值
>
> - 作用：告诉你当前鼠标位置或选中区域的像素亮度水平





> 5、图像特征
>
> ### 1. Mean (平均值) = 163.916
>
> - 含义：图像中所有像素的平均亮度值
>
> - 解读：163.916相对较低（最大值是1023），说明图像整体偏暗
>
> - 作用：判断图像的整体明亮程度
>
> ### 2. SD (标准差) = 148.025
>
> - 含义：像素亮度值的离散程度
>
> - 解读：148.025表示亮度变化较大，图像对比度较高
>
> - 作用：衡量图像的对比度和细节丰富程度
>
> ### 3. Minimum (最小值) = 2
>
> - 含义：图像中最暗像素的亮度值
>
> - 解读：2非常接近0（纯黑），说明图像有几乎纯黑的区域
>
> - 作用：了解图像的最暗部分
>
> ### 4. Maximum (最大值) = 1023
>
> - 含义：图像中最亮像素的亮度值
>
> - 解读：1023是最大值（纯白），说明图像有纯白或接近纯白的区域
>
> - 作用：了解图像的最亮部分
>
> ## 综合分析
>
> 从这四个数值可以看出：
>
> - 图像整体偏暗（平均值163.916）
>
> - 对比度很高（标准差148.025）
>
> - 亮度范围很广（从2到1023）
>
> - 既有很暗的区域，也有很亮的区域







> 6、Linear（线性）和 Logarithmic（对数）——图像区别
>
> Linear（线性）和 Logarithmic（对数）的区别在于Y轴的显示方式：
>
> ## Linear（线性）模式
>
> 特点：
>
> - Y轴按等比例显示像素数量
>
> - 0, 14978, 29957, 44935, 59914 等刻度
>
> - 每个刻度间隔相等
>
> 适用场景：
>
> - 像素数量分布相对均匀
>
> - 需要精确比较不同亮度级别的像素数量
>
> - 大多数普通图像分析
>
> ## Logarithmic（对数）模式
>
> 特点：
>
> - Y轴按对数比例显示像素数量
>
> - 刻度间隔逐渐增大
>
> - 低像素数量区域被"拉伸"，高像素数量区域被"压缩"
>
> 适用场景：
>
> - 像素数量差异巨大（比如有几十万个像素在某个亮度，而其他亮度只有几个像素）
>
> - 需要看清低像素数量区域的细节
>
> - 高对比度图像分析
>
> ## 选择建议
>
> - 一般情况：用Linear
>
> - 像素数量差异很大：用Logarithmic
>
> - 需要分析暗部细节：用Logarithmic
>
> ## 对数（Logarithmic）的特点
>
> ### 核心特点：压缩大数值，拉伸小数值





> 7、水平线剖面图（Horizontal Line Profile）！
>
> ## 什么是线剖面图
>
> 线剖面图用来分析图像中特定一行像素的亮度变化，而不是像直方图那样分析整个图像的像素分布。
>
> ## 图表含义
>
> X轴： 像素编号（0-1279，共1280个像素）
>
> Y轴： 像素强度值（0-1023，从黑到白）
>
> 黑色曲线： 显示沿着这条水平线上每个像素的实际亮度值
>
> ## 从曲线可以看出什么
>
> 图像特征：
>
> - 大部分区域较暗（亮度值50-150）
>
> - 有几个明显的亮峰：
>
> - 200-250像素处：亮度约250-300
>
> - 450-650像素处：亮度约300-400
>
> - 800-850像素处：亮度约250-300
>
> ## 实际应用
>
> 这种图常用于：
>
> 1. 边缘检测：尖峰表示明显的边缘
>
> 1. 缺陷检测：异常峰值可能表示缺陷
>
> 1. 光照分析：评估光照是否均匀
>
> 1. 尺寸测量：通过峰值位置测量物体尺寸
>
> ## 与直方图的区别
>
> - 直方图：显示整个图像的亮度分布
>
> - 线剖面图：显示特定一条线上的亮度变化



> 8、曝光控制（Exposure）
>
> 曝光就是相机传感器接收光线的时间，就像人眼"看东西"的时间长短。
>
> ### 曝光时间越长：
>
> - 传感器接收的光线越多
>
> - 图像越亮
>
> - 但运动物体可能模糊
>
> ### 曝光时间越短：
>
> - 传感器接收的光线越少
>
> - 图像越暗
>
> - 但能清晰捕捉快速运动
>
> ## 为什么需要控制曝光
>
> 不同场景需要不同曝光：
>
> - 室内暗光：需要长曝光（比如100毫秒）
>
> - 室外强光：需要短曝光（比如1毫秒）
>
> - 快速运动：需要短曝光避免模糊
>
> 曝光时间直接决定了图像的亮度和清晰度！



> 9、帧率控制 (Frame-Rate Control)
>
> 开了自动曝光，就不能自动帧率
>
> 开了自动帧率，就不能自动曝光
>
> 曝光时间 vs 帧率：
>
> - 长曝光（如20ms）→ 低帧率（最多约50 FPS）
>
> - 短曝光（如6ms）→ 高帧率（可达165+ FPS）
>
> 为什么？
>
> - 曝光时间占用"拍摄时间"
>
> - 帧率 = 1秒 ÷ 总拍摄时间
>
> - 曝光时间越长，每秒能拍的帧数越少
>
> 高速运动拍摄：短曝光 + 高帧率
>
> 静态物体：长曝光 + 低帧率



> 10、Gain（增益）
>
> Gain（增益）就是电子信号放大器，它放大相机传感器输出的电信号。
>
> ## 具体工作原理
>
> ### 信号流程：
>
> 1. 光线 → 传感器 → 电信号
>
> 1. 电信号 → 增益放大器 → 放大后的信号
>
> 1. 放大后的信号 → 图像处理 → 最终图像
>
> ### 当前设置：0 dB
>
> - 0 dB = 无额外放大
>
> - 传感器输出多少，图像就显示多少
>
> ## 增益的作用和影响
>
> ### 提高增益（比如+10 dB）：
>
> - 优点：图像变亮，适合低光照环境
>
> - 缺点：噪声增加，图像质量下降
>
> ## Gain增大 = 提高亮度
>
>
> Thorlabs相机的增益是固定的
>
> ### 增益值含义：
>
> - +5.3 dB：最大增益，图像最亮，但噪声最多
>
> - 0 dB：无增益，原始亮度，质量最好
>
> - -5.6 dB：负增益，图像变暗，噪声最少
>
> - -7.5 dB：最小增益，图像最暗，质量最高



> 11、Frame
>
> 在 **相机和成像领域**，**frame（帧）** 是一个非常核心的概念，它的意思是 **一次完整的图像采集**。
>
> ### 与帧率（FPS）的关系
>
> - **帧率（Frame Per Second, FPS）** = 每秒采集的 frame 数
> - 举例：
>   - FPS = 30 → 每秒采集 30 帧
>   - 每帧时间 = 1 ÷ FPS = 1/30 秒 ≈ 33 ms
>
> Frame计数器的作用：
>
> - 跟踪拍摄进度：知道拍了多少张
>
> - 时间参考：结合FPS可以计算拍摄时长



> 12、偏振
>
> **偏振（Polarization）** 是光学里一个非常核心的概念。它描述的是 **光波的电场矢量在传播过程中的振动方向**。
>
> ------
>
> ## 1. 背景
>
> - 光是一种 **电磁波**，它的本质是电场和磁场相互垂直，并且垂直于传播方向的波。
> - 在光的传播方向上，**电场矢量的振动方向** 就是我们说的 **偏振状态**。
>
> ------
>
> ## 2. 常见的偏振类型
>
> 1. **线偏振（Linear Polarization）**
>    - 电场总是沿一个固定方向振动
>    - 🌰：光通过偏振片（Polaroid）后就是线偏振
> 2. **圆偏振（Circular Polarization）**
>    - 电场矢量随时间旋转，形成一个圆轨迹
>    - 分为左旋圆偏振（LCP）和右旋圆偏振（RCP）
> 3. **椭圆偏振（Elliptical Polarization）**
>    - 电场矢量随时间旋转，但轨迹是椭圆
>    - 圆偏振是椭圆偏振的一种特例
> 4. **非偏振光（Unpolarized Light）**
>    - 自然光（如太阳光、灯泡光），电场方向是随机的，没有固定方向
>
> ------
>
> ## 3. 直观理解
>
> 你可以想象一根绳子：
>
> - 把绳子在地面上左右摇 → 线偏振
> - 把绳子旋转着摇 → 圆偏振
> - 不规则乱摇 → 非偏振光
>
> ------
>
> ## 4. 偏振的应用
>
> - **显微镜**：偏振显微镜观察双折射材料
> - **液晶显示屏（LCD）**：依靠液晶改变偏振态来显示图像
> - **3D 电影眼镜**：利用左右眼不同的偏振光分离图像
> - **光通信**：光纤里用偏振来提高信号稳定性
> - **应力检测**：偏振光能显示透明材料中的应力分布
>
> ------
>
> ✅ 总结：
>
> > **偏振 = 光的电场矢量在传播方向上的振动方式**
> >
> > - 可以是固定方向（线偏振）、旋转方向（圆/椭圆偏振）或随机（非偏振）
> > - 在显微、通信、显示、测量中都有广泛应用
> >
> > ## 为什么需要测量偏振？
> >
> > 当光照射到材料表面时，材料会改变光的偏振特性。通过测量这种变化，我们可以了解：
> >
> > - 材料的厚度
> >
> > - 材料的折射率
> >
> > - 材料的表面特性
> >
> > - 薄膜的层数和厚度





