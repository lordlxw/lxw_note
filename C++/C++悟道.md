# C++学习



## 问题汇总

> 1、\#include "iostream" 和 #include <iostream> 区别？
>
> - ### #include <iostream>（推荐）
>
>   - 搜索路径：编译器在系统标准库目录中搜索
>
>   - 用途：包含C++标准库头文件
>
>   - 搜索顺序：
>
>   1. 系统标准库目录
>
>   1. 编译器安装目录
>
>   1. 环境变量指定的目录
>
>   ### #include "iostream"（不推荐）
>
>   - 搜索路径：编译器在当前项目目录中搜索
>
>   - 用途：包含用户自定义的头文件
>
>   - 搜索顺序：
>
>   1. 当前源文件所在目录
>
>   1. 项目包含目录
>
>   1. 系统标准库目录（最后）



> 2、为什么 #include <iostream> 能工作的原因
>
> ### 编译器搜索顺序：
>
> 1. 先搜索 $(VC_IncludePath) → 找到 iostream
>
> 1. 再搜索 $(WindowsSDK_IncludePath) → Windows相关头文件
>
> 1. 最后搜索项目目录
>
> ```cmd
>   C:\Program Files\Microsoft Visual Studio\2022\Community\VC\Tools\MSVC\14.xx.xxxxx\include
> ```
>
> 



> 3、头文件的作用
>
> ```cmd
> #pragma once
> 
> // 告诉编译器：这些函数存在，后面会定义
> void LogIn(const char* message);
> ```
>
> - 头文件主要包含声明
>
> - 但也可以包含：内联函数、模板、常量、宏等
>
> - 不能包含普通函数的完整实现
>
> - 目的是让多个 .cpp 文件共享这些声明和定义
>
>  每个 .cpp 文件编译后都会生成一个对应的 .obj 文件。



> 4、静态函数和内联函数
>
> C++中类名后面用 ::，对象名后面用 .



> 5、#ifndef
>
> - \#ifndef = 防止头文件重复包含
>
> - 就像给头文件加个"锁"，确保只包含一次
>
> - 现代C++推荐用 #pragma once，更简洁



> 6、VS里配置， 同一份源代码可以编译成两种格式
>
> ![image-20250817142208949](C:\Users\admin\AppData\Roaming\Typora\typora-user-images\image-20250817142208949.png)







> 7、动态库（dll）和静态库（lib）的区别
>
> ### 动态库(.dll)：
>
> - 运行时加载：程序运行时才加载
>
> - 共享内存：多个程序可以共享同一个DLL
>
> - 节省内存：多个程序使用同一个DLL时节省内存
>
> - 更新方便：可以单独更新DLL文件
>
> - 依赖关系：程序运行时需要DLL文件存在
>
> ### 静态库(.lib)：
>
> - 编译时链接：编译时就把代码复制到程序中
>
> - 独立运行：程序不依赖外部文件
>
> - 内存占用：每个程序都有完整的库代码副本
>
> - 更新困难：需要重新编译整个程序
>
> - 无依赖：程序可以独立运行
>
> ### 同一份源代码可以编译成两种格式





> 8、pragma什么意思？
>
> \#pragma 是C++的预处理指令，用来给编译器发送特殊指令！
>
> - \#pragma once = 防止重复包含
>
> - \#pragma comment = 链接器/编译器选项
> - \#pragma warning= 控制警告
> - \#pragma pack= 内存对齐
>
> - 功能强大但降低可移植性
>
> ```test
> #include <windows.h>
> 
> #pragma comment(lib, "user32.lib")
> #pragma comment(lib, "gdi32.lib")
> 
> int WINAPI WinMain(HINSTANCE hInstance, HINSTANCE hPrevInstance, 
>                    LPSTR lpCmdLine, int nCmdShow)
> {
>     MessageBox(NULL, L"Hello World!", L"标题", MB_OK);
>     return 0;
> }
> ```
>
>
> ```内存对齐
> #pragma pack(push, 1)          // 设置1字节对齐
> struct MyStruct {
>     char a;     // 1字节
>     int b;      // 4字节
> };
> #pragma pack(pop)               // 恢复默认对齐
> ```
>
> 





> 9、编译和链接
>
> Ctrl F7，编译就是生成obj
>
> 链接需要找到main函数。





> 10、既然bool只表示0，1为什么不能用bit
>
> 寻址的最小单位是 byte（字节）
>
> ```对齐
> struct Example {
>     bool flag1;     // 1字节
>     int number;     // 4字节，需要4字节对齐
>     bool flag2;     // 1字节
> };
> // 总大小：8字节（不是6字节）
> ```
>
> 







## Thread

> #include<thread>
>
> std::thread worker(Dowork);
>
> void Dowork
>
> {
>
> }
>
> ![image-20251026100402890](C:\Users\admin\AppData\Roaming\Typora\typora-user-images\image-20251026100402890.png)





## 多维数组的释放问题

> ## 三、指针数组的多维分配（最常见但容易出错）
>
> 这种写法很多初学者一开始会用：
>  每一行单独 `new` 一块内存。
>
> ### ✅ 分配：
>
> ```
> int rows = 3, cols = 4;
> int** arr = new int*[rows];  // 先分配行指针数组
> for (int i = 0; i < rows; ++i) {
>     arr[i] = new int[cols]; // 每行再分配一个 int 数组
> }
> ```
>
> ### ✅ 使用：
>
> ```
> arr[0][1] = 42;
> ```
>
> ### ✅ 释放（⚠️ 很关键）：
>
> ```
> for (int i = 0; i < rows; ++i) {
>     delete[] arr[i];   // 先释放每一行
> }
> delete[] arr;           // 再释放行指针数组本身
> ```
>
> 🚨 注意：
>
> - 必须“分配顺序的逆序释放”。
> - 如果少释放任何一层，就会造成内存泄漏。